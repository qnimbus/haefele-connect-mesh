{
  "config": {
    "step": {
      "user": {
        "title": "Choose Connection Type",
        "data": {
          "connection_type": "Connection type"
        },
        "data_description": {
          "connection_type": "Local MQTT uses your existing MQTT broker for real-time local control. Cloud uses the Häfele cloud API."
        }
      },
      "mqtt_setup": {
        "title": "Configure Local MQTT",
        "data": {
          "use_ha_mqtt": "Use Home Assistant MQTT",
          "topic_prefix": "MQTT topic prefix",
          "polling_enabled": "Enable periodic polling",
          "poll_interval": "Polling interval"
        },
        "data_description": {
          "use_ha_mqtt": "Use the MQTT broker configured in Home Assistant. Disable to connect to a different broker.",
          "topic_prefix": "The MQTT topic prefix configured in your Häfele Gateway (default: hafele)",
          "polling_enabled": "Periodically request device state via MQTT. Recommended as a fallback for missed push updates.",
          "poll_interval": "How often to actively poll device state (seconds). Minimum 10 s."
        }
      },
      "mqtt_broker": {
        "title": "MQTT Broker Details",
        "data": {
          "broker": "Broker host",
          "port": "Port",
          "username": "Username",
          "password": "Password"
        },
        "data_description": {
          "broker": "Hostname or IP address of your MQTT broker",
          "port": "MQTT broker port (default: 1883)",
          "username": "Leave empty if authentication is not required",
          "password": "Leave empty if authentication is not required"
        }
      },
      "cloud_credentials": {
        "title": "Connect to Häfele Connect Mesh",
        "data": {
          "api_token": "API Token"
        }
      },
      "network": {
        "title": "Select Network",
        "description": "Found {device_count} devices across all networks.\nSelect a network to continue...",
        "data": {
          "network_id": "Network (Nr. of devices)"
        }
      },
      "reauth_confirm": {
        "title": "Reauthenticate Integration",
        "description": "The Häfele Connect Mesh integration needs to reauthenticate your account",
        "data": {
          "api_token": "API Token"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "no_networks_found": "No networks found",
      "network_not_found": "Selected network not found",
      "mqtt_not_configured": "MQTT integration is not configured. Please add the MQTT integration first.",
      "unknown": "Unexpected error"
    },
    "abort": {
      "reauth_successful": "Reauthentication was successful"
    },
    "selector": {
      "connection_type": {
        "options": {
          "mqtt": "Local MQTT (Recommended)",
          "cloud": "Cloud (Legacy)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Polling Options",
        "data": {
          "polling_enabled": "Enable periodic polling",
          "poll_interval": "Polling interval"
        },
        "data_description": {
          "polling_enabled": "Periodically request device state via MQTT. Recommended as a fallback for missed push updates.",
          "poll_interval": "How often to actively poll device state (seconds). Minimum 10 s."
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "last_update_success": {
        "name": "Most recent update status",
        "state": {
          "on": "Success",
          "off": "Failed"
        }
      }
    },
    "sensor": {
      "last_update": {
        "name": "Most recent update"
      }
    },
    "switch": {
      "socket": {
        "name": "Socket"
      }
    }
  }
}
